FROM node:18-alpine

WORKDIR /app

# Install only the backend runtime dependencies from the root package manifest.
COPY package*.json ./
RUN npm ci --omit=dev

# Copy the server entry point and backend source.
COPY server.js ./
COPY backend ./backend

EXPOSE 4000

CMD ["npm", "start"]
